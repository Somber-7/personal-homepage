# 배포 가이드 — Vercel + Neon (비용 0원)

AWS도 도메인도 필요 없다. Vercel Hobby 플랜은 무료이고 `프로젝트이름.vercel.app` 주소가 같이 나온다.
DB는 Neon 무료 플랜(영구 무료, 신용카드 불필요, 0.5GB)을 쓴다. 이 프로젝트는 텍스트만 저장하므로 넉넉하다.

전체 30분 정도 걸린다. 순서대로만 하면 된다.

---

## 0. 코드 쪽에서 이미 바꿔 둔 것

| 항목 | 내용 |
|---|---|
| DB | Prisma `provider`를 `mysql` → `postgresql`로 변경, `directUrl` 추가 |
| 마이그레이션 | 기존 MySQL 마이그레이션은 `prisma/migrations.mysql.bak`으로 옮김. Postgres에서는 `prisma db push`로 새로 만든다 |
| 비밀번호 | SHA-256 1회 해싱 → **bcrypt(cost 12)** 로 교체. `lib/auth.ts` |
| 관리자 계정 | seed에 박혀 있던 `admin / 1234` 제거. 이제 `ADMIN_PASSWORD` 환경변수로 넘겨야만 생성된다 (10자 미만이면 에러) |
| 빌드 | `build` 스크립트에 `prisma generate` 추가 (Vercel에서 필수) |
| 내용 | `prisma/seed.ts`를 경력 3건 · 프로젝트 15건 · 기술 5분류 · 자격증 8건으로 새로 작성 |
| 화면 문구 | 히어로·소개·통계의 "AI캠프 수강 중"을 "수료 · 최우수상"으로 갱신 |

---

## 1. Neon에서 데이터베이스 만들기

1. <https://neon.com> 가입 (GitHub 계정으로 가입하면 편하다)
2. **New Project** → 이름은 `portfolio` 정도, Region은 **`AWS Asia Pacific 1 (Singapore)`** 를 고른다
   (Neon에 서울 리전은 없다. 한국에서 가장 가까운 곳이 싱가포르다.)
   Services는 **Postgres database만 켜고 나머지는 전부 끈다.** Neon Auth는 로그인을 Neon에 맡기는 기능인데
   이 프로젝트는 NextAuth를 쓰므로 켜면 충돌한다.
3. 만들어지면 **Connection string** 화면이 나온다. 여기서 주소를 **두 개** 복사한다.
   - **Pooled connection** 체크된 주소 (호스트에 `-pooler`가 붙어 있다) → `DATABASE_URL`
   - **Pooled connection** 체크를 끈 주소 (직결) → `DIRECT_URL`

> 두 개를 쓰는 이유: Vercel은 요청마다 함수가 뜨는 서버리스 구조라 평소 쿼리는 풀링 주소로 나가야 커넥션이 마르지 않는다.
> 반면 `prisma db push` 같은 스키마 변경은 직결 주소로 해야 한다.

## 2. 로컬 `.env` 채우기

```powershell
copy .env.example .env
```

`.env`를 열어 1번에서 복사한 주소 두 개를 넣고, `NEXTAUTH_SECRET`은 아래로 새로 만들어 붙인다.

```powershell
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

`NEXTAUTH_URL`은 로컬에서는 `http://localhost:3000` 그대로 둔다.

## 3. 테이블 만들고 내용 채우기

```powershell
npx prisma db push
```

그다음 관리자 비밀번호를 정해서 seed를 돌린다. **PowerShell 기준**이다.

```powershell
$env:ADMIN_PASSWORD="여기에_직접_정한_10자_이상_비밀번호"
npm run db:seed
```

`seed 완료 — 관리자 계정: admin` 이 찍히면 된다.
(아이디를 바꾸고 싶으면 `$env:ADMIN_USERNAME="원하는아이디"`도 같이 넣는다.)

## 4. 로컬에서 확인

```powershell
npm run dev
```

- <http://localhost:3000> — 경력·프로젝트가 채워져 보이는지
- <http://localhost:3000/admin/login> — 3번에서 정한 계정으로 로그인되는지

## 5. GitHub에 올리기

`git init`은 이미 실행해 뒀다. 바로 스테이징하면 된다.

```powershell
git add .
git status
```

**`git status` 결과에 `.env`가 없는지 반드시 눈으로 확인한다.** `.gitignore`가 `.env*`를 막고 있어서 정상이라면 안 보인다.

```powershell
git commit -m "포트폴리오 홈페이지 초기 커밋"
```

GitHub에서 새 저장소를 만든 다음(Private으로 시작해도 되고, 나중에 Public으로 바꾸면 된다),

```powershell
git remote add origin https://github.com/<본인계정>/personal-homepage.git
git branch -M main
git push -u origin main
```

## 6. Vercel에 올리기

1. Vercel의 **Import Git Repository**에서 **GitHub**를 누르고 방금 만든 저장소를 고른다
2. Framework는 Next.js로 자동 인식된다. 건드릴 것 없다
3. **Environment Variables**에 네 개를 넣는다

   | Key | Value |
   |---|---|
   | `DATABASE_URL` | Neon 풀링 주소 |
   | `DIRECT_URL` | Neon 직결 주소 |
   | `NEXTAUTH_SECRET` | 2번에서 만든 문자열 |
   | `NEXTAUTH_URL` | 일단 `https://personal-homepage.vercel.app` 처럼 예상 주소를 넣어 둔다 |

4. **Deploy**

> 배포가 끝난 뒤 시간이 남으면, Vercel 프로젝트의 **Settings → Functions**에서 실행 리전을 Singapore로 맞춰 두면
> DB와 같은 지역에 붙어 쿼리가 빨라진다. 기본값(미국)으로 둬도 포트폴리오 규모에서는 체감 차이가 크지 않으니
> 옵션이 안 보이면 그냥 넘어가도 된다.

## 7. 마무리

배포가 끝나면 실제 주소가 나온다. 그 주소가 6번에 넣은 `NEXTAUTH_URL`과 다르면
**Settings → Environment Variables**에서 실제 주소로 고치고 **Redeploy** 한다.
이걸 안 맞추면 화면은 뜨는데 관리자 로그인만 안 된다.

이후로는 코드를 고쳐서 `git push` 하면 Vercel이 알아서 다시 배포한다.
내용 수정은 코드를 건드릴 필요 없이 `/admin/login`에서 하면 된다.

---

## 막힐 때

| 증상 | 확인할 것 |
|---|---|
| 빌드 실패, `PrismaClient did not initialize` | `package.json`의 `build`가 `prisma generate && next build`인지 |
| 화면은 뜨는데 로그인만 안 됨 | `NEXTAUTH_URL`이 실제 배포 주소와 정확히 같은지 (끝에 `/` 붙이지 말 것) |
| 첫 화면이 500 | `DATABASE_URL`이 `-pooler` 붙은 풀링 주소인지 |
| 화면이 비어 있음 | seed를 안 돌렸거나 다른 DB에 돌렸을 수 있다. Neon 콘솔의 Tables에서 행이 있는지 확인 |
| `db push`에서 권한 오류 | `DIRECT_URL`(직결 주소)이 제대로 들어갔는지 |
| 한참 안 들어가다가 첫 접속이 느림 | Neon 무료 플랜은 놀고 있으면 DB가 절전으로 내려간다(Scales to zero). 첫 요청에서 깨어나느라 1초 안팎 걸리고 그다음부터는 정상이다. 정상 동작이니 손댈 것 없다 |

## 나중에 할 것

- 저장소를 Public으로 바꿔서 채용 담당자가 코드를 볼 수 있게 하기
- 프로젝트 카드에 실제 화면 캡처 넣기 (지금은 텍스트만 나온다)
- 이력서·경력기술서 PDF 다운로드 버튼 추가
